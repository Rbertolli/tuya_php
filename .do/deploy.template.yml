spec:
  name: tuya-php-app
  features:
    container: true
  services:
    - name: tuya-php-functions
      type: container
      image:
        repository: ghcr.io/Rbertolli/tuya_php
        tag: latest
      ports:
        - container_port: 8080
      routes:
        - path: /
